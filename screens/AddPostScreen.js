import { Text, View } from 'react-native';

/*
    AddPostScreen is a screen that holds forms with current date prefilled.
    Select between 2 types of posts (captioned photo post or journal entry post).
*/
function AddPostScreen() {
    return (
        <View>
            <Text>Add a new post</Text>
        </View>
    );
}

export default AddPostScreen;
